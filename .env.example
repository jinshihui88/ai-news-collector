# DeepSeek API Key (必填)
# 用于 LLM 智能评分和过滤新闻内容
# 获取地址：https://platform.deepseek.com/api_keys
# 成本：Input $0.27/1M tokens, Output $1.10/1M tokens
# 预估：每次运行约 $0.01-0.02
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# 微信公众号 Token 和 Cookie (可选)
# 获取方式：详见 docs/how-to-get-wechat-token.md
# 1. 浏览器登录 https://mp.weixin.qq.com/
# 2. 从 URL 复制 token 参数值
# 3. 从 DevTools -> Network -> Headers -> Cookie 复制完整 Cookie
WECHAT_TOKEN=1234567890
WECHAT_COOKIE='xxxxxxxxxxx'

# 知识星球 Cookie (可选)
# 获取方式：
# 1. 浏览器登录 https://wx.zsxq.com
# 2. 打开开发者工具 (F12) -> Network 标签
# 3. 刷新页面,找到请求 https://api.zsxq.com 开头的请求(例如 topics 请求)
# 4. 点击该请求,在右侧 Headers 面板中找到 "Cookie:" 请求头
# 5. 复制完整的 Cookie 值(必须包含 zsxq_access_token 字段)
# 注意: 不要使用 document.cookie,因为 zsxq_access_token 可能是 HttpOnly Cookie
ZSXQ_COOKIE='xxxxxxxxxxx'

# Composio API
# 1. 登录 https://app.composio.dev/settings 获取 API Key
# 2. 在 Connections 页面选择 Twitter 连接,复制 Connection ID (形如 ca_xxx)
# 3. 在连接详情中查找 user_id 字段,填入 COMPOSIO_USER_ID
# 注意: 连接 ID 与 user_id 必须来自同一条连接
COMPOSIO_API_KEY=xxxxxxxxx
COMPOSIO_CONNECTION_ID=xxxxxxxxx
COMPOSIO_USER_ID=xxxxxxxxx
